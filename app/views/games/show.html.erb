
<h1 class="text-4xl font-extrabold mb-8 text-center text-indigo-600">
  Play Pong
</h1>
<p class="mb-4 text-center text-lg text-gray-700">
  Use W and S keys or tap the buttons below to move.
</p>


<div
  data-controller="pong"
  data-pong-submit-url-value="<%= scores_path %>"
  class="w-full sm:w-1/2 mx-auto px-4"
>

  <p class="inline-block bg-white bg-opacity-75 px-4 py-2 rounded-full
             text-xl font-semibold text-indigo-600 shadow-md mb-4">
    Score: <span data-pong-target="score" class="font-mono">0</span>
  </p>


  <div class="relative w-full pb-[75%] rounded-lg overflow-hidden bg-white shadow-inner">
    <canvas
      data-pong-target="canvas"
      class="absolute inset-0 w-full h-full"
    ></canvas>
  </div>


  <div class="flex justify-between mt-6 space-x-4">
    <button
      data-action="
        pointerdown->pong#moveUp
        pointerup->pong#stopMove
        pointerleave->pong#stopMove
      "
      class="flex-1 py-4 bg-green-400 hover:bg-green-500
             rounded-full text-3xl text-white shadow-lg
             transform hover:scale-105 transition duration-150"
    >
      ↑
    </button>

    <button
      data-action="
        pointerdown->pong#moveDown
        pointerup->pong#stopMove
        pointerleave->pong#stopMove
      "
      class="flex-1 py-4 bg-red-400 hover:bg-red-500
             rounded-full text-3xl text-white shadow-lg
             transform hover:scale-105 transition duration-150"
    >
      ↓
    </button>
  </div>
</div>
